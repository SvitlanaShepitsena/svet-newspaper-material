div
    ul.list-inline(ng-app='demo1', ng-controller='GalleryCtrl' id="gallery")
        li(ng-repeat='image in images')
            a(ng-click='openLightboxModal($index)')
                img.img-thumbnail(ng-src='{{image.thumbUrl}}', alt='')
