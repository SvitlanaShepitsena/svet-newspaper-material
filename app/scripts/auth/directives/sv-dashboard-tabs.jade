div
    md-tabs(md-selected='selectedIndex', md-border-bottom='' sv-dynamic-heght)
        md-tab.sv-tabs(ng-repeat='tab in tabs', label='{{tab.title|translate}}' )
            md-content.p(class='tab{{$index%4}}' ng-include='tab.content')
