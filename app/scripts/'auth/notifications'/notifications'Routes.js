(function () {
    'use strict'

    angular.module(''auth.notifications'', [])
    .config(function ($stateProvider, $urlRouterProvider) {
        $stateProvider

//#state'

    });
})();

