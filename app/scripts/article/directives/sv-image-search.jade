div
    form(name="searchForm")
        input( type='text' name='q' ng-model='q.data')
        md-button(ng-click='runGoogleSearch(q.data)') Search
    div
        h3 Results
        ul
            li(ng-repeat='i in images')
                img(ng-src='{{i.url}}')